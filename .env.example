# Copy this file to .env and adjust if your app runs on different ports.
# Start the app under test with `npm run dev` (from node-express-sequelize-realworld-example-app)
# before running this test suite.

API_BASE_URL=http://localhost:3000/api/
UI_BASE_URL=http://localhost:4101
